version: "3.9"
services:
  localweight:
    build: .
    container_name: localweight
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=file:../data/localweight.db
    restart: unless-stopped
